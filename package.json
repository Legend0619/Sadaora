{
  "name": "sadaora-starter-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npx prisma generate && npm start && cd frontend && npm ci && npm run build",
    "vercel-build": "concurrently \"cd backend && npx prisma generate && npm start\" \"cd frontend && npm ci && npm run build\"",
    "postinstall": "cd backend && npx prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "joi": "^17.11.0",
    "multer": "^1.4.5-lts.1",
    "aws-sdk": "^2.1490.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1",
    "@prisma/client": "^5.6.0",
    "uuid": "^9.0.1",
    "prisma": "^5.6.0",
  }
}